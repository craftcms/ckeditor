{% import '_includes/forms' as forms %}

{{ forms.autosuggestField({
  label: 'CKEditor Build URL'|t('ckeditor'),
  instructions: 'The URL to {file}. CKEditor 4 and 5 are both supported.'|t('ckeditor', {
    file: '`ckeditor.js`',
  }),
  id: 'build-url',
  name: 'buildUrl',
  value: settings.buildUrl,
  first: true,
  required: true,
  suggestEnvVars: true,
  suggestAliases: true,
  errors: settings.getErrors('buildUrl'),
}) }}
